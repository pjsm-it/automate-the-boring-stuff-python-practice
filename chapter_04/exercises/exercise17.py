# copy.copy() creates a shallow copy - creates a new outer object,
#  but the elements inside are still references/pointers
#  to the original objects.
# copy.deepcopy() creates a deep copy - creates a independent
#  copy that does not affect the original.
